## 1. 实现任务

- [x] 1.1 后端将音频源别名 `name_id` 的生成策略切换为 UUID（保持字段名与接口不变）。
- [x] 1.2 调整导入流程：同 `sha256` 的重复导入始终生成新的 UUID `name_id`，不再扫描空位复用旧值。
- [x] 1.3 在 `keySound` 播放解析链路中加入 `sha256 + name_id + type` 严格存在性校验，禁止按 `sha256+type` 隐式恢复引用。
- [x] 1.4 前端依赖检查与编辑器展示保持与新语义一致（缺失引用保持缺失，需手动重选才能恢复）。

## 2. 验证任务

- [x] 2.1 增加/更新 Go 测试：覆盖同文件多次导入、删除后重导入，验证 `name_id` 为唯一 UUID 且不复用。
- [x] 2.2 增加/更新依赖校验相关测试（前端或可替代验证脚本）：验证缺失引用不会因重导入自动变为可用。
- [x] 2.3 回归验证：重命名/删除音频源文件、创建/编辑裁剪声音、SSE 列表映射与自然排序行为不退化。

## 3. 发布与兼容

- [x] 3.1 记录配置兼容说明：历史数字 `name_id` 与新增 UUID `name_id` 的混合兼容规则。
- [x] 3.2 在变更说明中明确用户可见行为：删除依赖后必须重新选择源文件才能恢复。

## 验证记录

- 已执行：`cd sdk && go test ./server ./keySound`
- 结果：通过（覆盖 UUID name_id 生成与严格三元引用校验的核心逻辑）